# Variables de entorno requeridas por p1-backend.
# Copiar este archivo como .env y completar con valores reales.
# Se irán agregando variables a medida que se configuren

DATABASE_URL="postgresql://usuario:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require"
DIRECT_URL="postgresql://usuario:password@host.region.aws.neon.tech/dbname?sslmode=require"
